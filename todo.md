add a display tab:
    adjust brightnss
        auto brightness and a toggle to turn it on and off
    change resolution (only 4 options)
    change fps only 2 options
    change scaling
    screen rotation on/off/lock and manually pick one of 4
        auto rotation(https://github.com/YoRyan/legion-go-umpc)

make controller remapping UI better
make power led work
make gyro UI better and make it actually work